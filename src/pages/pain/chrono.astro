---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ChronoPain from '../../components/ChronoPain.svelte';
---

<BaseLayout
  title="Chrono Pain"
  description="Minuteur pas à pas pour la boulangerie maison — pétrissage, pointage, apprêt, cuisson."
>
  <div class="chrono-hero">
    <div class="hero-ornament" aria-hidden="true">
      <svg width="180" height="180" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width=".4">
        <circle cx="8" cy="8" r="5.5"/>
        <line x1="8" y1="8" x2="8" y2="4"/>
        <line x1="8" y1="8" x2="11" y2="9.5"/>
        <line x1="8" y1="2.5" x2="8" y2="2"/>
        <line x1="8" y1="14" x2="8" y2="13.5"/>
        <line x1="2.5" y1="8" x2="2" y2="8"/>
        <line x1="14" y1="8" x2="13.5" y2="8"/>
      </svg>
    </div>
    <div class="hero-inner">
      <div class="hero-rule"></div>
      <h1>Chrono Pain</h1>
      <p class="hero-intro">
        Configurez votre protocole, démarrez le minuteur, et suivez chaque étape de la fournée.
      </p>
      <div class="hero-links">
        <a href="../" class="hero-back">← Journal pain</a>
        <a href="aide/" class="hero-back">? Aide</a>
      </div>
    </div>
  </div>

  <div class="content">
    <ChronoPain client:load />
  </div>
</BaseLayout>

<style>
  .chrono-hero {
    background: var(--forest);
    padding: 3rem 2rem 2.5rem;
    border-bottom: 3px solid var(--copper);
    position: relative;
    overflow: hidden;
  }

  .hero-ornament {
    position: absolute;
    right: 3rem;
    top: 50%;
    transform: translateY(-50%);
    opacity: .07;
    pointer-events: none;
    color: var(--paper);
  }

  .hero-inner {
    max-width: 820px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .hero-rule {
    width: 40px;
    height: 2px;
    background: var(--copper);
    margin-bottom: 1rem;
  }

  h1 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(1.8rem, 4vw, 2.6rem);
    font-weight: 700;
    color: var(--paper);
    margin-bottom: .7rem;
  }

  .hero-intro {
    font-style: italic;
    color: rgba(245,237,216,.6);
    font-size: .95rem;
    max-width: 480px;
    line-height: 1.7;
    margin-bottom: 1rem;
  }

  .hero-back {
    font-family: 'Lora', serif;
    font-size: .8rem;
    color: rgba(245,237,216,.5);
    text-decoration: none;
    letter-spacing: .04em;
    transition: color .15s;
  }
  .hero-back:hover { color: var(--copper-lt); }

  .hero-links {
    display: flex;
    gap: 1.5rem;
  }

  .content {
    max-width: 820px;
    margin: 0 auto;
    padding: 2rem 2rem 4rem;
  }
</style>
